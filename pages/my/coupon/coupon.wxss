.coupon {
  /* padding: 20rpx 40rpx; */
  padding: 20rpx;
}
.wrapper {
  margin: 0 auto;
  width: 100%;
  display: flex;
  margin-bottom:20rpx;
}
/*前半部分样式*/
.content {
  position: relative;
  flex: 1;
  padding: 20rpx 30rpx;
  text-align: center;
}
/*后半部分样式*/
.tip {
  position: relative;
  padding:  50rpx 30rpx;
  flex: 0 0 200rpx;
  text-align: center;
}
/*中间竖直虚线样式*/
.split-line {
    position: relative;
    flex: 0 0 0;
    margin: 0 10rpx 0 6rpx;
    border-left: 4rpx dashed #f5f5f5;   
}
/*给虚线加两个伪类，基本样式如下*/
.split-line:before, .split-line:after {
    content: '';
    position: absolute;
    width: 32rpx;
    height: 16rpx;
    background: #f5f5f5;
    left: -18rpx;
    z-index: 1;
}
/*给前半部分加两个伪类，基本样式如下*/
.content:before, .content:after{
    content: '';
    position: absolute;
    width: 32rpx;
    height: 16rpx;
    background: #f5f5f5;
    left: -16rpx;
    z-index: 1;
}

/*给前半部分加两个伪类，基本样式如下*/
.tip:before, .tip:after{
    content: '';
    position: absolute;
    width: 32rpx;
    height: 16rpx;
    background:#f5f5f5;
    right: -16rpx;
    z-index: 1;
}
/*几个伪类化成的圆弧的样式以及位置（置于顶部）我把它放在一起了*/
.content:before, .tip:before, .split-line:before{
    border-radius: 0 0 16rpx 16rpx;
    top: 0;
}

/*几个伪类化成的圆弧的样式以及位置（置于底部）我把它放在一起了*/
.content:after, .tip:after, .split-line:after{
    border-radius: 16rpx 16rpx 0 0;
    bottom: 0;
}

.money {
  font-size: 110rpx;
  color: #f5f5f5;
}
.money text {
  font-size: 50rpx;
  padding-right: 20rpx; 
}
.title {
  color: #f5f5f5;
  font-size: 24rpx;
}
.conditions {
  color: #f5f5f5;
  font-size: 28rpx;
  padding:15rpx;
}
.useNow {
  font-size: 28rpx;
  border-radius: 50rpx;
  border-style: none;
}
.co {
  padding-top:10rpx; 
}
.co .co_w {
  width: 64rpx;
  height: 1rpx;
  background: #fff;
  margin-left: 40rpx;
  margin-bottom: -20rpx;
}

.co .co_l {
  width: 64rpx;
  height: 1rpx;
  background: #fff;
  margin-left: 240rpx;
  margin-top: -15rpx;
}
button:not([size='mini']) {
  width: 120px;
  margin-left: auto;
  margin-right: auto;
}
/* .colour-bg1{
   background:linear-gradient(-90deg,rgb(219, 147, 59),rgba(254,50,103,1));
} */
.colour-bg1{
   background:linear-gradient(-90deg,#ff6f89,#ff4860);
}
.colour-text1{
  color:#ff4860;
}
.colour-bg2{
   background:linear-gradient(-90deg,#7ac4ff,#2b7bff);
}
.colour-text2{
  color:#2b7bff;
}
.colour-bg3{
   background:linear-gradient(-90deg,#ffbb15,#ff8c1f);
}
.colour-text3{
  color:#ff8c1f;
}
.colour-bg4{
   background:linear-gradient(-90deg,#999999,#666666);
}
.colour-text4{
  color:#666666;
}
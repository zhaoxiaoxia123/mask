<view>

  <!-- 读取公众号对应文章 -->
 <!-- <swiper class="slide-bolk" indicator-dots="true" indicator-color="#999" indicator-active-color="#333" autoplay="{{false}}">
    <block wx:for="{{materialList}}" wx:key="*this" wx:for-item="material">
      <block wx:for="{{material.img_url}}" wx:for-item="item1" wx:key="*this">
        <swiper-item data-href="{{material.href}}" bindtap="goPost">
          <image src="{{item1}}" class="slide-image" />
        </swiper-item>
      </block>
    </block>
  </swiper> -->

  <!-- <view bindtap="test">test</view> -->
<!-- <web-view src="https://mp.weixin.qq.com/s/1eHAl1mx6-MPsTdtQTuCEw" wx:if=""></web-view> -->
  <!--滑块容器相关配置，以下为参数  广告"-->
  <swiper class="slide-bolk" indicator-dots="{{indicatorDots}}" indicator-color="{{indicatorColor}}" indicator-active-color="{{indicatorActiveColor}}" autoplay="{{true}}">
    <!--滑块容器内只能有swiper-item组件，并且属性默认为item.属性名   mode="widthFix"-->
    <block wx:for="{{swipers}}" wx:key="*this" wx:for-item="item1">
      <swiper-item wx:if="{{item1.type == 9}}">
        <image mode="widthFix" src="{{item1.img_url[0]}}" class="slide-image" bindtap="goPost" data-id="{{item1.post_id}}" data-href="{{item1.href}}" wx:if="{{item1.is_href == 1}}"/>
        <image mode="widthFix" src="{{item1.img_url[0]}}" class="slide-image" wx:if="{{item1.is_href == 2}}"/>
        <image mode="widthFix" src="{{item1.img_url[0]}}" data-href="{{item1.href}}" class="slide-image" wx:if="{{item1.is_href == 3}}" bindtap="goMyPage"/>
      </swiper-item>
    </block>
  </swiper>
  <!--滑块容器相关配置，以下为参数   其他广告-->
  <!-- <swiper class="slide-bolk" indicator-dots="true" indicator-color="#999" indicator-active-color="#333" autoplay="{{false}}">
    <block wx:for="{{swipers}}" wx:key="*this">
      <swiper-item wx:if="{{item.type == 12}}">
        <image src="{{item.b_image_url}}" class="slide-image" />
      </swiper-item>
    </block>
  </swiper> -->
  
  <!-- <view class="detail-image">
    <block wx:for="{{swipers}}" wx:key="*this" >
      <image wx:if="{{item.type == 12}}" mode="widthFix" src="{{item.b_image_url}}" class="slide-image" bindtap='toPost' data-obj='{{item.post_id}}' />
    </block>
  </view> -->
  
  <!-- <view class="ad-member">
    <image class="member-img" src='../img/member/member@2x.png'></image>
  </view>
  <view class="ad-member">
    <image class="member-img" src='../img/member/member1@2x.png'></image>
  </view>
  <view class='lists  page-pd'>
    <view class="list" bindtap='toDetail' wx:key="*this" wx:for="{{items}}" wx:for-index="index" wx:for-item="item" data-obj='{{item}}'>
      <view class='list-left'>
        <image src='{{item.image_url[0]}}' lazy-load></image>
      </view>
      <view class='list-right'>
        <view>
          <view class='right-text'>{{item.category_desc}}</view>
        </view>
        <view>
          <view class='abstract'>{{item.frozeno_introduction?item.frozeno_introduction:''}}</view>
        </view>
        <view class='column'>
          <view class='sale'>{{item.frozeno_discount_amount}}</view>
          <view class='money'>{{item.frozeno_amount}}</view>
        </view>
        <view class='list-button'>
          <text>立即购买</text>
        </view>
      </view>
    </view>
  </view> -->
  
  <view class="detail-image">
    <block wx:for="{{items}}" wx:key="*this" wx:for-index="index" wx:for-item="item">
      <image mode="widthFix" src="{{item.image_url[0]}}" lazy-load bindtap='toDetail' data-obj='{{item}}'/>
    </block>
  </view>

  <view class="detail-image">
    <block wx:for="{{swipers}}" wx:key="*this" >
      <image wx:if="{{item.type == 13}}" mode="widthFix" src="{{item.img_url[0]}}" class="slide-image"/>
    </block>
  </view>
  <view class="detail-ft">
    <view class="detail-ft-item">
    <!-- 需要判断是否是会员，是否下发了优惠券 -->
        <view class="ft-item" bindtap="goLogin"></view>
        <view class="ft-item" bindtap="information"></view>
    </view>
    <view class="detail-ft-item">
        <view class="ft-item" bindtap="grade"></view>
        <view class="ft-item" bindtap="goMember"></view>
    </view>
  </view>
</view>

<!--滑块容器相关配置，以下为参数   关于我们广告-->
 <!-- <swiper class="slide-bolk" indicator-dots="true" indicator-color="#999" indicator-active-color="#333" autoplay="{{false}}">
    <block wx:for="{{swipers}}" wx:key="*this">
      <swiper-item wx:if="{{item.type == 13}}">
        <image src="{{item.b_image_url}}" class="slide-image" />
      </swiper-item>
    </block>
  </swiper> -->

  <!-- <button bindtap="goWx">go mp weixin</button> -->
<!-- 声明需要使用的模板文件 -->
<import src="../template/template.wxml" />
<view class='item' bindtap='ftservice' data-item='{{ftserviceflexwindow}}'>
  <template is="service" data="{{ftserviceflexwindow}}" />
</view>

<!-- 引导页分两步 -->
 <view class="just1 {{storeflexwindow==true?'': 'y'}}" >
  <view class="guide {{step == 1?'':'zy'}}">
    <view class="guide-item" style="padding-top:20rpx;padding:20rpx;margin-top:80px;">
      <image class="guide-img" mode="widthFix" src="../img/guide-t@2x.png"></image>
    </view>
    <view class="guide-ft">
        <view class="guide-btn" data-step='2' bindtap='close3'>下一步 1/2</view>
    </view>
  </view>
  <view class="guide {{step == 2?'':'zy'}}">
    <view class="guide-item guide-s" >
      <image class="guide-simg" mode="widthFix" src="../img/guide-s@2x.png"></image>
    </view>
    <view class="guide-ft guide-sft">
        <view class="guide-btn" data-step='3' bindtap='close3'>知道啦 2/2</view>
    </view>
  </view>
    
</view>